name: 🚀 Daily AI Blog Automation - Draft Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install anthropic requests google-api-python-client google-auth-oauthlib google-auth-httplib2 ddgs

      - name: 🤖 Run Automation Script in Draft Mode
        env:
          CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
          CLAUDE_MODEL: ${{ secrets.CLAUDE_MODEL }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          BLOGGER_CLIENT_ID: ${{ secrets.BLOGGER_CLIENT_ID }}
          BLOGGER_CLIENT_SECRET: ${{ secrets.BLOGGER_CLIENT_SECRET }}
          BLOGGER_REFRESH_TOKEN: ${{ secrets.BLOGGER_REFRESH_TOKEN }}
          BLOG_ID: ${{ secrets.BLOG_ID }}
          DRAFT_MODE: "true"
          HIGH_RISK_DRAFT_MODE: "true"
        run: python main.py

      - name: 📦 Upload draft backup
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: draft-backups
          path: draft_backups/*
          if-no-files-found: ignore
